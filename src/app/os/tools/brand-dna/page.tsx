'use client';

import React from 'react';
import IdentityEngine from '@/components/tools/IdentityEngine';

/**
 * FLAT SCREEN MICROTOOL: Brand DNA Extraction
 * This is a standalone, one-shot utility.
 * It uses the independent IdentityEngine/Store journey.
 */
export default function BrandDNAMicrotoolPage() {
  return (
    <div className="pt-20">
      <IdentityEngine />
    </div>
  );
}